---
title: "Vue Bootstrap Dropdown Component"
name: "Dropdown with Bootstrap Styling"
bootstrap: true
description: "Learn how to use CoreUI’s Vue Dropdown component with Bootstrap styles for flexible, framework-consistent UI."
---

import DropdownSingleButtonExample from './examples/DropdownSingleButtonExample.vue'
import DropdownSingleButtonExampleRaw from './examples/DropdownSingleButtonExample.vue?raw'
import DropdownSingleButton2Example from './examples/DropdownSingleButton2Example.vue'
import DropdownSingleButton2ExampleRaw from './examples/DropdownSingleButton2Example.vue?raw'
import DropdownSingleButton3Example from './examples/DropdownSingleButton3Example.vue'
import DropdownSingleButton3ExampleRaw from './examples/DropdownSingleButton3Example.vue?raw'
import DropdownSplitButtonExample from './examples/DropdownSplitButtonExample.vue'
import DropdownSplitButtonExampleRaw from './examples/DropdownSplitButtonExample.vue?raw'
import DropdownSizingLargeExample from './examples/DropdownSizingLargeExample.vue'
import DropdownSizingLargeExampleRaw from './examples/DropdownSizingLargeExample.vue?raw'
import DropdownSizingSmallExample from './examples/DropdownSizingSmallExample.vue'
import DropdownSizingSmallExampleRaw from './examples/DropdownSizingSmallExample.vue?raw'
import DropdownDarkExample from './examples/DropdownDarkExample.vue'
import DropdownDarkExampleRaw from './examples/DropdownDarkExample.vue?raw'
import DropdownDark2Example from './examples/DropdownDark2Example.vue'
import DropdownDark2ExampleRaw from './examples/DropdownDark2Example.vue?raw'
import DropdownCenteredExample from './examples/DropdownCenteredExample.vue'
import DropdownCenteredExampleRaw from './examples/DropdownCenteredExample.vue?raw'
import DropdownDropupExample from './examples/DropdownDropupExample.vue'
import DropdownDropupExampleRaw from './examples/DropdownDropupExample.vue?raw'
import DropdownDropupCenteredExample from './examples/DropdownDropupCenteredExample.vue'
import DropdownDropupCenteredExampleRaw from './examples/DropdownDropupCenteredExample.vue?raw'
import DropdownDropendExample from './examples/DropdownDropendExample.vue'
import DropdownDropendExampleRaw from './examples/DropdownDropendExample.vue?raw'
import DropdownDropstartExample from './examples/DropdownDropstartExample.vue'
import DropdownDropstartExampleRaw from './examples/DropdownDropstartExample.vue?raw'
import DropdownResponsiveAlignmentExample from './examples/DropdownResponsiveAlignmentExample.vue'
import DropdownResponsiveAlignmentExampleRaw from './examples/DropdownResponsiveAlignmentExample.vue?raw'
import DropdownResponsiveAlignment2Example from './examples/DropdownResponsiveAlignment2Example.vue'
import DropdownResponsiveAlignment2ExampleRaw from './examples/DropdownResponsiveAlignment2Example.vue?raw'
import DropdownMenuContentHeadersExample from './examples/DropdownMenuContentHeadersExample.vue'
import DropdownMenuContentHeadersExampleRaw from './examples/DropdownMenuContentHeadersExample.vue?raw'
import DropdownMenuContentDividersExample from './examples/DropdownMenuContentDividersExample.vue'
import DropdownMenuContentDividersExampleRaw from './examples/DropdownMenuContentDividersExample.vue?raw'
import DropdownMenuContentTextExample from './examples/DropdownMenuContentTextExample.vue'
import DropdownMenuContentTextExampleRaw from './examples/DropdownMenuContentTextExample.vue?raw'
import DropdownMenuContentFormsExample from './examples/DropdownMenuContentFormsExample.vue'
import DropdownMenuContentFormsExampleRaw from './examples/DropdownMenuContentFormsExample.vue?raw'
import DropdownOptionsExample from './examples/DropdownOptionsExample.vue'
import DropdownOptionsExampleRaw from './examples/DropdownOptionsExample.vue?raw'
import DropdownOptionsAutoCloseBehaviorExample from './examples/DropdownOptionsAutoCloseBehaviorExample.vue'
import DropdownOptionsAutoCloseBehaviorExampleRaw from './examples/DropdownOptionsAutoCloseBehaviorExample.vue?raw'

## Overview

Dropdowns are toggleable, contextual overlays for displaying lists of links and more.

Dropdowns are built on a third party library, [Popper.js](https://popper.js.org/), which provides dynamic positioning and viewport detection. Popper.js isn't used to position dropdowns in navbars though as dynamic positioning isn't required.

## Examples

Bind the dropdown's toggle and the dropdown menu inside `<CDropdown>`, or different element that declares `position: relative;`. Dropdowns can be triggered from `<a>` or `<button>` elements to better fit your possible requirements.

### Single button

Here's how you can put them to work with either `<button>` elements:
<Example class="theme-bootstrap" code={DropdownSingleButtonExampleRaw} lang="vue" name="DropdownSingleButtonExample">
  <DropdownSingleButtonExample client:only="vue" />
</Example>

And with `<a>` elements:

<Example class="theme-bootstrap" code={DropdownSingleButton2ExampleRaw} lang="vue" name="DropdownSingleButton2Example">
  <DropdownSingleButton2Example client:only="vue" />
</Example>

The best part is you can do this with any button variant, too:

<Example class="theme-bootstrap" code={DropdownSingleButton3ExampleRaw} lang="vue" name="DropdownSingleButton3Example">
  <DropdownSingleButton3Example client:only="vue" />
</Example>

### Split button

Similarly, create split button dropdowns with virtually the same markup as single button dropdowns, but with the addition of boolean prop `split` for proper spacing around the dropdown caret.

We use this extra class to reduce the horizontal `padding` on either side of the caret by 25% and remove the `margin-left` that's attached for normal button dropdowns. Those additional changes hold the caret centered in the split button and implement a more properly sized hit area next to the main button.

<Example class="theme-bootstrap" code={DropdownSplitButtonExampleRaw} lang="vue" name="DropdownSplitButtonExample">
  <DropdownSplitButtonExample client:only="vue" />
</Example>

## Sizing

Button dropdowns work with buttons of all sizes, including default and split dropdown buttons.

<Example class="theme-bootstrap" code={DropdownSizingLargeExampleRaw} lang="vue" name="DropdownSizingLargeExample">
  <DropdownSizingLargeExample client:only="vue" />
</Example>

<Example class="theme-bootstrap" code={DropdownSizingSmallExampleRaw} lang="vue" name="DropdownSizingSmallExample">
  <DropdownSizingSmallExample client:only="vue" />
</Example>

## Dark dropdowns

Opt into darker dropdowns to match a dark navbar or custom style by set `dark` property. No changes are required to the dropdown items.

<Example class="theme-bootstrap" code={DropdownDarkExampleRaw} lang="vue" name="DropdownDarkExample">
  <DropdownDarkExample client:only="vue" />
</Example>

And putting it to use in a navbar:

<Example class="theme-bootstrap" code={DropdownDark2ExampleRaw} lang="vue" name="DropdownDark2Example">
  <DropdownDark2Example client:only="vue" />
</Example>

## Directions

<Callout color="info" title="RTL">
  Directions are mirrored when using CoreUI in RTL, meaning `.dropstart` will appear on the right side.
</Callout>

### Centered

Make the dropdown menu centered below the toggle by adding `direction="center"` to the `<CDropdown>` component.

<Example class="theme-bootstrap" code={DropdownCenteredExampleRaw} lang="vue" name="DropdownCenteredExample">
  <DropdownCenteredExample client:only="vue" />
</Example>

### Dropup

Trigger dropdown menus above elements by adding `direction="dropup"` to the `<CDropdown>` component.

<Example class="theme-bootstrap" code={DropdownDropupExampleRaw} lang="vue" name="DropdownDropupExample">
  <DropdownDropupExample client:only="vue" />
</Example>

### Dropup centered

Make the dropup menu centered above the toggle by adding `direction="dropup-center"` to the `<CDropdown>` component.

<Example class="theme-bootstrap" code={DropdownDropupCenteredExampleRaw} lang="vue" name="DropdownDropupCenteredExample">
  <DropdownDropupCenteredExample client:only="vue" />
</Example>

### Dropend

Trigger dropdown menus at the right of the elements by adding `direction="dropend"` to the `<CDropdown>` component.

<Example class="theme-bootstrap" code={DropdownDropendExampleRaw} lang="vue" name="DropdownDropendExample">
  <DropdownDropendExample client:only="vue" />
</Example>

### Dropstart

Trigger dropdown menus at the left of the elements by adding `direction="dropstart"` to the `<CDropdown>` component.

<Example class="theme-bootstrap" code={DropdownDropstartExampleRaw} lang="vue" name="DropdownDropstartExample">
  <DropdownDropstartExample client:only="vue" />
</Example>

### Responsive alignment

If you use responsive alignment, dynamic positioning is disabled.

To align **right** the dropdown menu with the given breakpoint or larger, add `aligment="xs|sm|md|lg|xl|xxl: end"`.

<Example class="theme-bootstrap" code={DropdownResponsiveAlignmentExampleRaw} lang="vue" name="DropdownResponsiveAlignmentExample">
  <DropdownResponsiveAlignmentExample client:only="vue" />
</Example>

To align **left** the dropdown menu with the given breakpoint or larger, add `aligment="xs|sm|md|lg|xl|xxl: start"`.

<Example class="theme-bootstrap" code={DropdownResponsiveAlignment2ExampleRaw} lang="vue" name="DropdownResponsiveAlignment2Example">
  <DropdownResponsiveAlignment2Example client:only="vue" />
</Example>

## Menu content

### Headers

Add a header to label sections of actions in any dropdown menu.

In the following example we use `div` instead of `<CDropdownMenu>` to show `<CDropdownMenu>` content.

<Example class="theme-bootstrap" code={DropdownMenuContentHeadersExampleRaw} lang="vue" name="DropdownMenuContentHeadersExample">
  <DropdownMenuContentHeadersExample client:only="vue" />
</Example>

### Dividers

Separate groups of related menu items with a divider.

In the following example we use `div` instead of `<CDropdownMenu>` to show `<CDropdownMenu>` content.

<Example class="theme-bootstrap" code={DropdownMenuContentDividersExampleRaw} lang="vue" name="DropdownMenuContentDividersExample">
  <DropdownMenuContentDividersExample client:only="vue" />
</Example>
### Text

Place any freeform text within a dropdown menu with text. Note that you'll likely need additional sizing styles to constrain the menu width.

<Example class="theme-bootstrap" code={DropdownMenuContentTextExampleRaw} lang="vue" name="DropdownMenuContentTextExample">
  <DropdownMenuContentTextExample client:only="vue" />
</Example>

### Forms

Put a form within a dropdown menu, or make it into a dropdown menu.

<Example class="theme-bootstrap" code={DropdownMenuContentFormsExampleRaw} lang="vue" name="DropdownMenuContentFormsExample">
  <DropdownMenuContentFormsExample client:only="vue" />
</Example>

## Dropdown options

Use `offset` to displace the dropdown from its default position. The value is a string with two numbers separated by a comma, e.g. `:offset="[10, 20]"`. Use `teleport` property to render dropdowns in `body` instead of the parent element. This helps to avoid any overflow or z-index issues.

<Example class="theme-bootstrap" code={DropdownOptionsExampleRaw} lang="vue" name="DropdownOptionsExample">
  <DropdownOptionsExample client:only="vue" />
</Example>

### Auto close behavior

By default, dropdowns are closed when clicking outside of the dropdown menu or the toggle button. You can change this behavior with the `autoClose` property. Set `autoClose` to: 

- `true` - Close on clicks inside or outside of the Vue.js dropdown menu.
- `false` - Disable auto-close; close manually by setting the `:visible="false"` (also not closed by `Escape`).
- `'inside'` - Close only when clicking inside the Vue.js dropdown menu.
- `'outside'` - Close only when clicking outside the Vue.js dropdown menu.

<Example class="theme-bootstrap" code={DropdownOptionsAutoCloseBehaviorExampleRaw} lang="vue" name="DropdownOptionsAutoCloseBehaviorExample">
  <DropdownOptionsAutoCloseBehaviorExample client:only="vue" />
</Example>
