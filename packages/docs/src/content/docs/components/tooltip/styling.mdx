---
title: "Vue Tooltip Component Styling"
name: "Tooltip Styling"
description: "Learn how to customize the Vue Tooltip component with CSS classes, variables, and SASS for flexible styling and seamless integration into your design."
---

### CSS variables

Vue toltips use local CSS variables on `.tooltip` for enhanced real-time customization. Values for the CSS variables are set via Sass, so Sass customization is still supported, too.

<ScssDocs file="_tooltip.scss" capture="tooltip-css-vars"/>

#### How to use CSS variables

```vue
<template>
  <CTooltip :style="vars">...</CTooltip>
</template>

<script setup>
const vars = {
  '--cui-tooltip-color': '#333',
  '--cui-tooltip-bg': '#f8f9fa',
}
</script>
```

### SASS variables

<ScssDocs file="_variables.scss" capture="tooltip-variables"/>
