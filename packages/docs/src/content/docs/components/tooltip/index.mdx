---
title: "Vue Tooltip Component and Directive"
name: "Tooltip"
description: "Documentation and examples for adding Vue Tooltips."
otherFrameworks:
  - tooltip
---

import TooltipOnLinksExample from './examples/TooltipOnLinksExample.vue'
import TooltipOnLinksExampleRaw from './examples/TooltipOnLinksExample.vue?raw'
import TooltipDirectionsExample from './examples/TooltipDirectionsExample.vue'
import TooltipDirectionsExampleRaw from './examples/TooltipDirectionsExample.vue?raw'
import TooltipDirectiveExample from './examples/TooltipDirectiveExample.vue'
import TooltipDirectiveExampleRaw from './examples/TooltipDirectiveExample.vue?raw'
import TooltipCustomExample from './examples/TooltipCustomExample.vue'
import TooltipCustomExampleRaw from './examples/TooltipCustomExample.vue?raw'
import TooltipDisabledElementsExample from './examples/TooltipDisabledElementsExample.vue'
import TooltipDisabledElementsExampleRaw from './examples/TooltipDisabledElementsExample.vue?raw'

## Examples

### Tooltips on links

Hover over the links below to see tooltips:

<Example code={TooltipOnLinksExampleRaw} name="TooltipOnLinksExample">
  <TooltipOnLinksExample client:only="vue" />
</Example>

### Component

Hover over the buttons below to see the four tooltips directions: top, right, bottom, and left. Directions are mirrored when using CoreUI in RTL.

<Example code={TooltipDirectionsExampleRaw} name="TooltipDirectionsExample">
  <TooltipDirectionsExample client:only="vue" />
</Example>

### Directive

<Example code={TooltipDirectiveExampleRaw} name="TooltipDirectiveExample">
  <TooltipDirectiveExample client:only="vue" />
</Example>

### Custom tooltips

You can customize the appearance of tooltips using [CSS variables](#css-variables). We set a custom `style` to scope our custom appearance and use it to override some of the local CSS variables.

<Example code={TooltipCustomExampleRaw} name="TooltipCustomExample">
  <TooltipCustomExample client:only="vue" />
</Example>

## Usage

A shown tooltip can be dismissed by pressing the <kbd>Escape</kbd> key, helping satisfy the [WCAG 1.4.13 "Content on Hover or Focus"](https://www.w3.org/WAI/WCAG21/Understanding/content-on-hover-or-focus.html) success criterion.

### Disabled elements

Elements with the disabled attribute aren’t interactive, meaning users cannot focus, hover, or click them to trigger a tooltip (or popover). As a workaround, you’ll want to trigger the tooltip from a wrapper `<div>` or `<span>`, ideally made keyboard-focusable using `tabindex="0"`.

<Example code={TooltipDisabledElementsExampleRaw} name="TooltipDisabledElementsExample">
  <TooltipDisabledElementsExample client:only="vue" />
</Example>

## API

Check out the documentation below for a comprehensive guide to all the props you can use with the components mentioned here.

- [&lt;CTooltip /&gt;](./api/#ctooltip)
