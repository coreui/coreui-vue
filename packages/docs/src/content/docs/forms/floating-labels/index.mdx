---
title: "Vue Floating labels"
name: "Floating labels"
description: "Vue floating label component. Create beautifully simple form labels that float over your input fields."
otherFrameworks:
  - floating-labels
---

import FloatingLabelsExample from './examples/FloatingLabelsExample.vue'
import FloatingLabelsExampleRaw from './examples/FloatingLabelsExample.vue?raw'
import FloatingLabels2Example from './examples/FloatingLabels2Example.vue'
import FloatingLabels2ExampleRaw from './examples/FloatingLabels2Example.vue?raw'
import FloatingLabelsValidationExample from './examples/FloatingLabelsValidationExample.vue'
import FloatingLabelsValidationExampleRaw from './examples/FloatingLabelsValidationExample.vue?raw'
import FloatingLabelsTextareaExample from './examples/FloatingLabelsTextareaExample.vue'
import FloatingLabelsTextareaExampleRaw from './examples/FloatingLabelsTextareaExample.vue?raw'
import FloatingLabelsTextarea2Example from './examples/FloatingLabelsTextarea2Example.vue'
import FloatingLabelsTextarea2ExampleRaw from './examples/FloatingLabelsTextarea2Example.vue?raw'
import FloatingLabelsSelectExample from './examples/FloatingLabelsSelectExample.vue'
import FloatingLabelsSelectExampleRaw from './examples/FloatingLabelsSelectExample.vue?raw'
import FloatingLabelsLayoutExample from './examples/FloatingLabelsLayoutExample.vue'
import FloatingLabelsLayoutExampleRaw from './examples/FloatingLabelsLayoutExample.vue?raw'

## Example
Use `floatingLabel` property on `<CFormInput>`, `<CFormSelect>` or `<CFormTextarea>` to enable floating labels with textual form fields. A `placeholder` is required on each `<CFormInput>`, `<CFormSelect>` and `<CFormTextarea>` as our method of CSS-only floating labels uses the `:placeholder-shown` pseudo-element.

<Example code={FloatingLabelsExampleRaw} lang="vue" name="FloatingLabelsExample">
  <FloatingLabelsExample client:only="vue" />
</Example>

You can create the same form control by wrapping a pair of `<CFormInput>` and `<CFormLabel>` elements in `<CFormFloating>` to enable floating labels with textual form fields. A `placeholder` is required on each `<CFormInput>` as our method of CSS-only floating labels uses the `:placeholder-shown` pseudo-element. Also, note that the `<CFormInput>` must come first so we can utilize a sibling selector (e.g., `~`).

```vue
<CFormFloating class="mb-3">
  <CFormInput type="email" id="floatingInput" placeholder="name@example.com" />
  <CFormLabel for="floatingInput">Email address</CFormLabel>
</CFormFloating>
<CFormFloating>
  <CFormInput type="password" id="floatingPassword" placeholder="Password" />
  <CFormLabel for="exampleFormControlPassword">Password</CFormLabel>
</CFormFloating>
```

When there's a `value` already defined, `<CFormLabel>`s will automatically adjust to their floated position.

<Example code={FloatingLabels2ExampleRaw} lang="vue" name="FloatingLabels2Example">
  <FloatingLabels2Example client:only="vue" />
</Example>

Form validation styles also work as expected.

<Example code={FloatingLabelsValidationExampleRaw} lang="vue" name="FloatingLabelsValidationExample">
  <FloatingLabelsValidationExample client:only="vue" />
</Example>

## Textareas

By default, `<CFormTextarea>`s will be the same height as `<CFormInput>`s.

<Example code={FloatingLabelsTextareaExampleRaw} lang="vue" name="FloatingLabelsTextareaExample">
  <FloatingLabelsTextareaExample client:only="vue" />
</Example>

To set a custom height on your `<CFormTextarea>`, do not use the `rows` attribute. Instead, set an explicit `height` (either inline or via custom CSS).

<Example code={FloatingLabelsTextarea2ExampleRaw} lang="vue" name="FloatingLabelsTextarea2Example">
  <FloatingLabelsTextarea2Example client:only="vue" />
</Example>

## Selects

Other than `<CFormInput>`, floating labels are only available on `<CFormSelect>`s. They work in the same way, but unlike `<CFormInput>`s, they'll always show the `<CFormLabel>` in its floated state. **Selects with `size` and `multiple` are not supported.**

<Example code={FloatingLabelsSelectExampleRaw} lang="vue" name="FloatingLabelsSelectExample">
  <FloatingLabelsSelectExample client:only="vue" />
</Example>

## Layout

When working with the CoreUI for Bootstrap grid system, be sure to place form elements within column classes.

<Example code={FloatingLabelsLayoutExampleRaw} lang="vue" name="FloatingLabelsLayoutExample">
  <FloatingLabelsLayoutExample client:only="vue" />
</Example>
